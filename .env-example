# Default number of objects per page
PER_PAGE = 9

# Django secret key
SECRET_KEY = ']rmub)uaEte@[!-pSJb=%BPfV>^,#^"/hLAH^*vZ,@j"pP+oBk.uSWRv+z4ji[Q'
# 0 = False e 1 =True
DEBUG = 0

# JWT
SECRET_KEY_JWT = 'lmBiL!XMCp:o=JqnD}tqxJHmgQLu#y"}H]p`OoxBdX}(a$kaeL#W*]^?ihZiQ%;L'

# Database settings
# sqlite
#DATABASE_ENGINE = 'django.db.backends.sqlite3'
#DATABASE_NAME = "./db.sqlite3"

# Postgres
DATABASE_ENGINE = 'django.db.backends.postgresql'
DATABASE_NAME = "change-me"

DATABASE_USER = "bauxita"
DATABASE_PASSWORD = "change-me
DATABASE_HOST = "change-me"
DATABASE_PORT = "change-me"

# MySQL
# DATABASE_ENGINE = 'django.db.backends.mysql'
# DATABASE_NAME = "basedados"

# DATABASE_USER = "usuario"
# DATABASE_PASSWORD = "senha"
# DATABASE_HOST = "127.0.0.1"
# DATABASE_PORT = "3306"

# Comma separated values

ALLOWED_HOSTS = '127.0.0.1, localhost'
CSRF_TRUSTED_ORIGINS = 'https://seu-dominio.com.br'

# Para gerar chaves seguras
# python -c "import string as s;from random import SystemRandom as sr;print(''.join(sr().choices(s.ascii_letters + s.punctuation, k=64)))"

CORS_ALLOWED_ORIGINS = 'http://change-me.com'
